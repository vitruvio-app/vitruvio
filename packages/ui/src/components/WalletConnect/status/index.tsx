import React from 'react'

export interface Props {
  showDisconnect: boolean
}

const WalletConnectStatus = (props: Props): JSX.Element => {
  return <div>Alive</div>
}

export default WalletConnectStatus
